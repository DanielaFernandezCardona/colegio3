machine:
  php:
    version: 5.6.17

  environment:
    APP_ENV: testing
    APP_KEY: base64:or42Gh8bxQIr3hfW2oMgVBv1CM/wAccQsTKOw7lCZV0=

general:
  artifacts:
    - "reports"

dependencies:
  pre:

  cache_directories:
    - "~/.composer/cache"
    - "reports/phplint/.cache"
    - "vendor"

test:
  override:
 # - vendor/bin/phpunit -d memory_limit=1024M --coverage-clover build/logs/clover.xml
   - mkdir -p $CIRCLE_TEST_REPORTS/phpunit
    - phpunit --log-junit $CIRCLE_TEST_REPORTS/phpunit/junit.xml tests